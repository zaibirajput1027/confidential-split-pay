import { task } from "hardhat/config";
import { extendWithVerificationArgs } from "./utils.js";
const verifyTask = extendWithVerificationArgs(task("verify", "Verify a contract on all supported explorers"))
    .setAction(() => import("./task-action.js"))
    .build();
export default verifyTask;
//# sourceMappingURL=index.js.map